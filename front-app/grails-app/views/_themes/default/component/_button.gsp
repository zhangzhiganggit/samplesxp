<button${id ? ' id=' + id : ''} type="button"
                                class="btn${type ? ' btn-' + type : ''}${color ? ' ' + color : ''}${outline == 'yes' ? ' btn-outline' : ''}${size ? ' btn-' + size : ''}${disabled ? ' ' + disabled : ''}">
    ${title}
</button>
